jQuery(document).ready(function ($) {
    $('.slicknav_icon').click(function () {
        // used to show the mobile menu on mobile only
        $('.mobile-nav').slideToggle('display-none');
    });
});